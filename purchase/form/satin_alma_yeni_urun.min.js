data = mergeCompanies(data); var ww_data = data; function mergeCompanies(data) { const result = []; data.forEach(entry => { const existing = result.find(c => c.COMPANY_ID === entry.COMPANY_ID); if (existing) { existing.URUNLER = existing.URUNLER.concat(entry.URUNLER); } else { result.push({ FULLNAME: entry.FULLNAME, COMPANY_ID: entry.COMPANY_ID, URUNLER: [...entry.URUNLER] }); } }); return result; }
var table = document.getElementById('price-table'), output = document.getElementById('output'), selectedCells = new Map, productSet = new Set, productInfoMap = new Map; data.forEach(supplier => { supplier.URUNLER.forEach(product => { productSet.add(product.PRODUCT_ID); if (!productInfoMap.has(product.PRODUCT_ID)) { productInfoMap.set(product.PRODUCT_ID, { PRODUCT_ID: product.PRODUCT_ID, STOCK_ID: product.STOCK_ID, PRODUCT_NAME: product.PRODUCT_NAME }); } }) }); const uniqueProducts = Array.from(productSet), headerRow = document.createElement('tr'); headerRow.innerHTML = `<th class="sticky-header bg-success text-white">&Uuml;r&uuml;n</th><th class="sticky-header bg-success text-white">Urun Kodu</th><th class="sticky-header bg-success text-white">Oem No</th>`; headerRow.innerHTML += `<th class="sticky-header bg-info text-white">Marj (%)<br><input id="global-marj-input" onchange="setMarjAllRows(this)"  type="number" class="form-control form-control-sm" style="width:80px; margin-top:4px;" placeholder="Toplu"></th>`; headerRow.innerHTML += `<th class="sticky-header bg-info text-white">Son Satış Fiyatı (${DEMAND_MONEY})</th>`; headerRow.innerHTML += `<th class="sticky-header bg-info text-white">Satış Fiyatı (${DEMAND_MONEY})</th>`; data.forEach(supplier => { const th = document.createElement('th'); th.className = "sticky-header bg-success text-white"; th.innerHTML = `${supplier.FULLNAME}<br><small>ID: ${supplier.COMPANY_ID}</small>`; headerRow.appendChild(th); }); const thead = document.createElement('thead'), tbody = document.createElement('tbody'); thead.appendChild(headerRow); table.appendChild(thead); const cellElements = {}, alternativeGroups = {}; data.forEach(supplier => { supplier.URUNLER.forEach(product => { const baseId = product.PRODUCT_ID, altIds = (product.ALTERNATIFLER || []).map(a => a.PRODUCT_ID), allIds = [baseId, ...altIds]; let existingColor = allIds.find(id => alternativeGroups[id]), groupColor = existingColor ? alternativeGroups[existingColor] : getRandomColor(); allIds.forEach(id => { alternativeGroups[id] = groupColor; }); }); }); uniqueProducts.forEach(productId => { const urunBilgisi = productInfoMap.get(productId), productName = urunBilgisi.PRODUCT_NAME, rowHasSatinalma = data.some(supplier => { const p = supplier.URUNLER.find(u => u.PRODUCT_ID === productId); return p?.IS_SATINALMA === 1; }), rowHasOS = data.some(supplier => { const p = supplier.URUNLER.find(u => u.PRODUCT_ID === productId); return p?.IS_OS === false; }), row = document.createElement('tr'), productCell = document.createElement('td'); productCell.innerHTML = `<a href="javascript:void(0)" onclick="window.open('http://qa.kdteknik.com.tr/index.cfm?fuseaction=objects.popup_product_price_history_js&sepet_process_type=2502&product_id=${urunBilgisi.PRODUCT_ID}&stock_id=${urunBilgisi.STOCK_ID}&pid=${urunBilgisi.PRODUCT_ID}&product_name=&unit=Adet&row_id=0&TL=1&USD=1.55&EUR=3','popup','width=800,height=600');">${productName}</a>`; productCell.className = 'product-name'; row.appendChild(productCell); const codeCell = document.createElement('td'); let productCode = ""; for (const supplier of data) { const product = supplier.URUNLER.find(p => p.PRODUCT_ID === productId); if (product && product.PRODUCT_CODE_2) { productCode = product.PRODUCT_CODE_2; break; } } let last_price = 0; for (const supplier of data) { const product = supplier.URUNLER.find(p => p.PRODUCT_ID === productId); if (product && product.LAST_PRICE) { last_price = product.LAST_PRICE; break; } } codeCell.textContent = productCode || "N/A"; codeCell.className = 'product-code'; row.appendChild(codeCell); const oemCell = document.createElement('td'); let oemNo = ""; for (const supplier of data) { const product = supplier.URUNLER.find(p => p.PRODUCT_ID === productId); if (product && product.OEM_NO) { oemNo = product.OEM_NO; break; } } oemCell.textContent = oemNo || "N/A"; oemCell.className = 'product-oem'; row.appendChild(oemCell); let slpInfo = {}; for (const supplier of data) { const match = supplier.URUNLER.find(p => p.PRODUCT_ID === productId && p.SLP && p.SLP.length > 0); if (match) { slpInfo = match.SLP[0]; break; } } const marjCell = document.createElement('td'), marjInput = document.createElement('input'); marjInput.type = 'number'; marjInput.value = slpInfo.PRODUCT_MARJ || 0; marjInput.className = 'form-control form-control-sm'; marjInput.style.width = '80px'; marjCell.appendChild(marjInput); const lastPriceCell = document.createElement('td'); if (last_price === 0) { lastPriceCell.textContent = "-"; } else { const currency = MONEYARRRR.find(c => c.MONEY === DEMAND_MONEY), rate1 = parseFloat(currency?.RATE1 || 1), rate2 = parseFloat(currency?.RATE2 || 1), convertedLastPrice = (last_price * rate1) / rate2; lastPriceCell.textContent = convertedLastPrice.toFixed(2); } marjCell.appendChild(marjInput); const salePriceCell = document.createElement('td'), salePriceInput = document.createElement('input'); salePriceInput.type = 'text'; salePriceInput.value = slpInfo.SALE_PRICE != null ? slpInfo.SALE_PRICE.toFixed(2) : ""; salePriceInput.className = 'form-control form-control-sm sale-price-input'; salePriceInput.style.width = '100px'; salePriceInput.dataset.product = productId; salePriceCell.appendChild(salePriceInput); marjInput.addEventListener('input', () => { const selectedKey = selectedCells.get(productId), net = parseFloat(selectedKey?.split('|')[6]), marj = parseFloat(marjInput.value); if (!isNaN(net) && !isNaN(marj)) { const calculatedSalePrice = net + net * marj / 100, otherMoney = selectedKey?.split('|')[9], currency = MONEYARRRR.find(c => c.MONEY === DEMAND_MONEY), rate1 = parseFloat(currency?.RATE1 || 1), rate2 = parseFloat(currency?.RATE2 || 1), converted = calculatedSalePrice * rate1 / rate2; salePriceInput.value = converted.toFixed(2); } }); salePriceInput.addEventListener('change', () => { const selectedKey = selectedCells.get(productId), salePrice = parseFloat(salePriceInput.value.replace(',', '.')) || 0, otherMoney = selectedKey?.split('|')[9], netPrice = parseFloat(selectedKey?.split('|')[6]), currency = MONEYARRRR.find(c => c.MONEY === DEMAND_MONEY), rate1 = parseFloat(currency?.RATE1 || 1), rate2 = parseFloat(currency?.RATE2 || 1), convertedSalePrice = salePrice * rate2 / rate1, smx = convertedSalePrice - netPrice, smx2 = smx / netPrice, smx3 = smx2 * 100; marjInput.value = smx3; console.table({ convertedSalePrice, smx, smx2, smx3, salePrice, netPrice }); }); row.appendChild(marjCell); row.appendChild(lastPriceCell); row.appendChild(salePriceCell); cellElements[productId] = []; let lowestNetPrice = Infinity; data.forEach(supplier => { const product = supplier.URUNLER.find(p => p.PRODUCT_ID === productId); if (product && product.NET_PRICE < lowestNetPrice) { lowestNetPrice = product.NET_PRICE; } }); data.forEach(supplier => { const product = supplier.URUNLER.find(p => p.PRODUCT_ID === productId), cell = document.createElement('td'); if (product) { const cmqurrency = MONEYARRRR.find(c => c.MONEY === DEMAND_MONEY), cmqrate1 = parseFloat(cmqurrency?.RATE1 || 1), cmqrate2 = parseFloat(cmqurrency?.RATE2 || 1), cmqconvertedPrice = parseFloat(product.NET_PRICE) / cmqrate2 * cmqrate1, cellKey = `${supplier.COMPANY_ID}|${product.PRODUCT_ID}|${product.PRICE}|${product.WRK_ROW_ID}|${product.DISCOUNT_1}|${product.QUANTITY}|${product.NET_PRICE}|${product.TAX}|${product.PRICE_OTHER}|${product.OTHER_MONEY}|${DEMAND_MONEY}|${product.STOCK_ID}|${productName}|${product.IS_SELECTED || 0}|${product.IS_SATINALMA || 0}|${product.OEM_NO || ""}|${product.SELECT_INFO_EXTRA || 0}`, priceDisplay = product.DISCOUNT_1 > 0 ? `<div class="tooltip"><span class="price-original">${product.PRICE_OTHER.toFixed(2)} ${product.OTHER_MONEY}</span><span class="tooltiptext">İskonto: ${product.DISCOUNT_1}%</span></div>` : `<div><strong>${product.PRICE_OTHER.toFixed(2)} ${product.OTHER_MONEY}</strong></div>`; let netPriceHtml = `<div class="net-price">Net: ${product.NET_PRICE.toFixed(2)} TL <div><strong>${cmqconvertedPrice.toFixed(2)} ${DEMAND_MONEY}</strong></div></div>`; if (product.NET_PRICE === lowestNetPrice) { netPriceHtml = `<div class="net-price">⭐ Net: ${product.NET_PRICE.toFixed(2)} TL <div><strong>${cmqconvertedPrice.toFixed(2)} ${DEMAND_MONEY}</strong></div></div>`; cell.title = 'En iyi teklif' + product.WRK_ROW_ID; } cell.innerHTML = `${priceDisplay}${netPriceHtml}<div class="text-muted small">Iskonto: ${product.DISCOUNT_1}%</div><div class="text-muted small">Adet: ${product.QUANTITY}</div>${product.IS_SELECTED === 1 ? '<div class="text-primary fw-bold small">✅ Seçili </div>' : ''}${product.IS_SATINALMA === 1 ? '<div class="text-danger fw-bold small">🚩 Satış Teklifinde</div>' : ''}${product.SNT_S === 1 ? '<div class="text-success fw-bold small">💵 S.Alma Oluştu</div>' : ''}${((product.IS_SATINALMA === 0 && product.IS_SELECTED === 1) && product.IS_OS === false) ? '<div class="text-warning fw-bold small">⚠️ Satış Teklifinden Kaldırıldı</div>' : ''}${product.BASKET_INFO_TYPE ? `<div class="text-info fw-bold small"> ${product.BASKET_INFO_TYPE}</div>` : ''}`; if (product.IS_OS === false) { console.log("Ürün Satış Teklifine Dönmüşmüş", product.PRODUCT_NAME, product.IS_OS); } if (rowHasOS) { console.log("Satırda  Satış Teklifine Dönmüşmüş Ürün Var", product.PRODUCT_NAME, product.IS_OS); } const bgColor = alternativeGroups[product.PRODUCT_ID]; if (product.IS_SATINALMA === 1) { $("#send-btn").hide(); $("#send-btn3").hide(); } if (!rowHasSatinalma && !rowHasOS) { cell.classList.add('selectable'); cell.dataset.key = cellKey; cell.addEventListener('click', () => { cellElements[productName].forEach(c => { const icon = c.querySelector('div.check-icon'); if (icon) icon.remove(); }); const checkIcon = document.createElement('div'); checkIcon.className = 'check-icon text-success'; checkIcon.innerHTML = '✔️'; cell.appendChild(checkIcon); selectedCells.set(productId, cellKey); updateOutput(); updateBestSupplier(); }); if (product.IS_SELECTED === 1) { if (product.IS_OS === true) { if (!rowHasOS) { const checkIcon = document.createElement('div'); checkIcon.className = 'check-icon text-success'; checkIcon.innerHTML = '✔️'; cell.appendChild(checkIcon); selectedCells.set(productId, cellKey); } else { cell.style.pointerEvents = 'none'; cell.style.opacity = '0.8'; cell.title = 'Bu ürün için satın alma yapılmış. Seçim yapılamaz.'; } } else { cell.style.pointerEvents = 'none'; cell.style.opacity = '0.8'; cell.title = 'Bu ürün için satın alma yapılmış. Seçim yapılamaz.'; } } } else { cell.style.pointerEvents = 'none'; cell.style.opacity = '0.8'; cell.title = 'Bu ürün için satın alma yapılmış. Seçim yapılamaz.'; } cell.dataset.product = productId; cell.addEventListener('click', () => { cellElements[productId].forEach(c => { const icon = c.querySelector('div.check-icon'); if (icon) icon.remove(); }); const checkIcon = document.createElement('div'); checkIcon.className = 'check-icon text-success'; checkIcon.innerHTML = '✔️'; cell.appendChild(checkIcon); selectedCells.set(productId, cellKey); updateOutput(); updateBestSupplier(); }); cellElements[productId].push(cell); if (product.IS_SELECTED === 1) { if (product.IS_OS === true) { const checkIcon = document.createElement('div'); checkIcon.className = 'check-icon text-success'; checkIcon.innerHTML = '✔️'; cell.appendChild(checkIcon); selectedCells.set(productId, cellKey); } } } else { cell.className = 'no-data'; cell.textContent = "-"; } row.appendChild(cell); }); tbody.appendChild(row); table.appendChild(tbody); }); function updateOutput() { const grouped = {}; selectedCells.forEach((key, productId) => { const [companyId, productIdStr, price, wrkRowId, discount1, quantity, netPrice, tax, priceOther, otherMoney, demandMoney, stockId, isSatinalma, yyy, xxx, oemNo, selectInfoExtra] = key.split('|'); if (!grouped[companyId]) { grouped[companyId] = { companyId: parseInt(companyId), products: [] }; } const urunBilgisi = productInfoMap.get(productId), productName = urunBilgisi?.PRODUCT_NAME || "", marjInput = [...document.querySelectorAll('td.product-name')].find(td => td.textContent.trim() === productName.trim())?.parentElement.querySelector('input'), salePriceInput = document.querySelector(`input.sale-price-input[data-product="${productId}"]`); let productMarj = 0, salePrice = 0; if (marjInput) { productMarj = parseFloat(marjInput.value) || 0; } if (salePriceInput) { salePrice = parseFloat(salePriceInput.value) || 0; } let convertedsalePriceOther = 0; try { const currency = MONEYARRRR.find(c => c.MONEY === demandMoney), rate1 = parseFloat(currency?.RATE1 || 1), rate2 = parseFloat(currency?.RATE2 || 1); convertedsalePriceOther = parseFloat(salePrice) * rate2 / rate1; } catch (e) { console.error("Hata:", e); convertedPriceOther = 0; } grouped[companyId].products.push({ productId: parseInt(productIdStr), stockId: parseInt(stockId), price: parseFloat(price), wrkRowId, discount1: parseFloat(discount1), quantity: parseFloat(quantity), netPrice: parseFloat(netPrice), tax: parseFloat(tax), priceOther: parseFloat(priceOther), otherMoney: otherMoney, productName: productName, isSatinalma: parseInt(isSatinalma), productMarj: productMarj, salePrice: parseFloat(salePrice.toFixed(2)), convertedsalePriceOther: parseFloat(convertedsalePriceOther.toFixed(2)), demandMoney: demandMoney, oemNo: oemNo, xxx: xxx, yyy: yyy, selectInfoExtra: selectInfoExtra, }); }); const groupedArray = Object.values(grouped); output.textContent = JSON.stringify(groupedArray, null, 2); return groupedArray; }
function updateBestSupplier() { const supplierTotals = {}; console.log(data); ww_data.forEach(supplier => { const productIds = supplier.URUNLER.map(p => p.PRODUCT_ID), hasAllProducts = uniqueProducts.every(pid => productIds.includes(pid)); if (!hasAllProducts) return; let total = 0; supplier.URUNLER.forEach(product => { if (product.NET_PRICE > 0) { total += product.NET_PRICE; } }); if (total > 0) { supplierTotals[supplier.FULLNAME] = total; } }); const [bestSupplierName, bestTotal] = Object.entries(supplierTotals).sort((a, b) => a[1] - b[1])[0] || ["Belirlenemedi", 0]; document.getElementById('best-supplier').textContent = `En iyi fiyat veren tedarikçi: ${bestSupplierName} (Toplam: ${bestTotal.toFixed(2)} TL)`; }
window.setMarjAllRows = function (el) { const newMarj = parseFloat(el.value) || 0; document.querySelectorAll('td input[type="number"]').forEach(input => { input.value = newMarj; input.dispatchEvent(new Event('input')); }); };
function getRandomColor() { const letters = '0123456789ABCDEF'; let color = '#'; for (let i = 0; i < 6; i++) { color += letters[Math.floor(Math.random() * 16)]; } return color; }
document.getElementById('send-btn').addEventListener('click', () => { if (!sifirKontrl()) { return; } const payload = updateOutput(); console.log("Sunucuya gönderilecek veri:", payload); var offer_id = document.getElementById("offer_id").value; fetch('/AddOns/Partner/purchase/cfc/purchase_service.cfc?method=savePurchaseOfferSelector', { method: 'POST', headers: { 'Content-Type': 'application/json; charset=UTF-8' }, body: JSON.stringify({ payload, offer_id, session_variables }) }).then(response => response.json()).then(data => { if (data.RES === "success") { alert("İşlem başarılı!"); window.location.reload(); } else if (data.RES === "error") { alert("Bir hata oluştu!"); } }).catch(error => { console.error("Hata:", error); alert("Sunucuya bağlanırken bir hata oluştu!"); }); });
document.getElementById('send-btn3').addEventListener('click', () => { updateOutput(); const payload = updateOutput(); console.log("Sunucuya gönderilecek veri:", payload); var offer_id = document.getElementById("offer_id").value, BEI = 0; fetch('/AddOns/Partner/purchase/cfc/purchase_service.cfc?method=savePurchaseOfferSelectorOnly', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ payload, offer_id, session_variables, BEI }) }).then(response => response.json()).then(data => { if (data.RES === "success") { alert("İşlem başarılı!"); AjaxPageLoad("index.cfm?fuseaction=sales.ajax_list_pbs_offer_purchases_1&INTERNAL_ID=" + offer_id, "ShownArea", 1, "Yükleniyor") } else if (data.RES === "error") { alert("Bir hata oluştu!"); } }).catch(error => { console.error("Hata:", error); alert("Sunucuya bağlanırken bir hata oluştu!"); }); });
updateBestSupplier();
function sifirKontrl() { var mx = updateOutput(), SFRFIYAT = 0; for (let i = 0; i < mx.length; i++) { var my = mx[i].products; for (let j = 0; j < my.length; j++) { var p = my[j].productMarj; if (p == 0) { SFRFIYAT++ } } } if (SFRFIYAT > 0) { var fx = confirm("Marj Girilmemiş Ürünler Var; Lütfen Kontrol Edin!"); return fx; } else { return true; } }
function SatinalmaSiparis(params) { fetch('/AddOns/Partner/purchase/cfc/purchase_service.cfc?method=SAVEORDER_gpt&internal_id=' + params, { method: 'POST', headers: { 'Content-Type': 'application/json' } }).then(response => response.json()).then(data => { if (data.RES === "success") { alert("İşlem başarılı!"); window.location.reload(); } else if (data.RES === "error") { alert("Bir hata oluştu!"); } }).catch(error => { console.error("Hata:", error); alert("Sunucuya bağlanırken bir hata oluştu!"); }); }
$(document).on("click", "#price-table > tbody > tr > td > span", function () { var $span = $(this), currentValue = $span.text(), $input = $("<input type='text' class='price-editor'>").val(currentValue); $span.replaceWith($input); $input.focus(); $input.on("blur", function () { var newValue = $input.val(); $input[0].dispatchEvent(new Event('input')); var $newSpan = $("<span>").text(commaSplit(newValue)); $input.replaceWith($newSpan); }); $input.on("keydown", function (e) { if (e.key === "Enter") { $(this).blur(); } }); });

